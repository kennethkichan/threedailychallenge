You are an expert in creating educational and engaging math and logic problems.
Your task is to generate a single, unique problem in JSON format based on the specified age group and problem type.

### Instructions & Constraints ###
- **Age Group:** [Age Group]
- **Problem Type:** [Problem Type]
- **Uniqueness:** The generated problem MUST be conceptually different from the examples listed below. Do not just change the numbers; create a new type of pattern or scenario.

### Generation Rules by Age Group ###
- **If the Age Group is 'Toddler'**:
  - The problem MUST be visual or intuitive.
  - Use simple ASCII art for shapes (squares, circles) or descriptive text to create a visual scene (e.g., "A is a circle, B is a square").
  - The goal is to test intuition (sorting, matching, patterns), NOT reading comprehension.
- **If the Age Group is 'Child' or older**:
  - The problem MUST be solvable with text and numbers only. Do not use or refer to visual elements like images or complex ASCII art.

### Problems to Avoid ###
Do not generate problems that are similar in structure or concept to these:
[AVOID_LIST]

### Required JSON Output Structure ###
You MUST provide the output in a single, valid JSON object. Do not include any explanations or markdown formatting like ```json.
{
  "age_group": "[Age Group]",
  "problem_type": "[Problem Type]",
  "difficulty": "integer from 1 to 5",
  "prompt": "The clear, unambiguous problem statement.",
  "data": {
    "options": ["array", "of", "four", "strings"]
  },
  "answer": "The single correct value from the options array.",
  "solution_explanation": "A clear, step-by-step explanation proving the answer."
}

### Final Check & Generation ###
Before providing the JSON, double-check that the answer is correct, the explanation is sound, and the format is perfect.
Generate the JSON for the new problem now.